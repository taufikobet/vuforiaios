vuforiaios
==========

Vuforia iOS SDK

1. Create a `Podfile` in your root project
2. Enter this:

```
   platform :ios
   
   pod 'VuforiaSDK', :git => 'git@github.com:taufikobet/vuforiaios.git'
```
